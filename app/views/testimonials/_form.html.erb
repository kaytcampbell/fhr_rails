<%= form_with(model: testimonial, local: true) do |form| %>
  <% if testimonial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testimonial.errors.count, "error") %> prohibited this testimonial from being saved:</h2>

      <ul>
      <% testimonial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body, id: :testimonial_body %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author, id: :testimonial_author %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, id: :testimonial_location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
